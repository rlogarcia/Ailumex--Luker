<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <data noupdate="1">
        <!-- Cron: Recalcular scores de leads diariamente -->
        <record id="cron_recalculate_lead_scores" model="ir.cron">
            <field name="name">Recalcular puntuación de leads</field>
            <field name="model_id" ref="model_crm_lead"/>
            <field name="state">code</field>
            <field name="code">model.cron_recalculate_lead_scores()</field>
            <field name="user_id" ref="base.user_admin"/>
            <field name="interval_number">1</field>
            <field name="interval_type">days</field>
            <field name="active">True</field>
        </record>

        <!-- Cron: Verificar oportunidades inactivas diariamente -->
        <record id="cron_check_inactive_opportunities" model="ir.cron">
            <field name="name">Verificar oportunidades inactivas</field>
            <field name="model_id" ref="model_crm_lead"/>
            <field name="state">code</field>
            <field name="code">model.cron_check_inactive_opportunities()</field>
            <field name="user_id" ref="base.user_admin"/>
            <field name="interval_number">1</field>
            <field name="interval_type">days</field>
            <field name="active">True</field>
        </record>
    </data>
</odoo>
