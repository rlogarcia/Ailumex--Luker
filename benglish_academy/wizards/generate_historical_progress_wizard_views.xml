<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_generate_historical_progress_wizard_form" model="ir.ui.view">
        <field name="name">benglish.generate.history.wizard.form</field>
        <field name="model">benglish.generate.history.wizard</field>
        <field name="arch" type="xml">
            <form string="Generar Historial Académico Retroactivo">
                <group>
                    <div class="alert alert-info" role="alert">
                        <p><strong>⚠️ IMPORTANTE: Para Estudiantes Importados</strong></p>
                        <p>Este wizard crea registros históricos en el historial académico para estudiantes
                        que fueron importados y ya tienen progreso (ej: están en Unit 2, 3, 10, 24, etc.).</p>
                        <p><strong>¿Qué hace?</strong></p>
                        <ul>
                            <li>Detecta la unidad actual de cada estudiante</li>
                            <li>Crea registros "attended" para TODAS las clases de unidades anteriores</li>
                            <li>Así el sistema calcula correctamente el progreso y no muestra clases ya vistas</li>
                        </ul>
                        <p><strong>Ejemplo:</strong> Si Adriana está en Unit 2, se crearán registros para todas las clases de Unit 1 (BChecks + Skills)</p>
                    </div>
                </group>
                
                <group>
                    <field name="student_ids" widget="many2many_tags"/>
                    <field name="historical_date"/>
                    <field name="dry_run"/>
                </group>
                
                <group string="Vista Previa">
                    <field name="preview_text" widget="text" nolabel="1"/>
                </group>
                
                <footer>
                    <button name="action_generate" string="Generar Historial" type="object" 
                            class="btn-primary"/>
                    <button string="Cancelar" class="btn-secondary" special="cancel"/>
                </footer>
            </form>
        </field>
    </record>

    <record id="action_generate_historical_progress_wizard" model="ir.actions.act_window">
        <field name="name">Generar Historial Retroactivo</field>
        <field name="res_model">benglish.generate.history.wizard</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
        <field name="binding_model_id" ref="benglish_academy.model_benglish_student"/>
        <field name="binding_view_types">list</field>
    </record>

</odoo>
