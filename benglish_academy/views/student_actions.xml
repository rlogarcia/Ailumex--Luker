<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- Acción Server para creación masiva de usuarios portal -->
    <record id="action_student_create_portal_users_mass" model="ir.actions.server">
        <field name="name">Crear Usuarios Portal (Masivo)</field>
        <field name="model_id" ref="model_benglish_student"/>
        <field name="binding_model_id" ref="model_benglish_student"/>
        <field name="binding_view_types">list</field>
        <field name="state">code</field>
        <field name="code">
if records:
    action = records.action_create_portal_user()
        </field>
    </record>

    <!-- Acción Server para eliminación definitiva masiva de estudiantes -->
    <record id="action_student_force_delete_mass" model="ir.actions.server">
        <field name="name">Eliminar Estudiantes (Masivo)</field>
        <field name="model_id" ref="model_benglish_student"/>
        <field name="binding_model_id" ref="model_benglish_student"/>
        <field name="binding_view_types">list</field>
        <field name="state">code</field>
        <field name="code">
if records:
    action = records.action_force_delete()
        </field>
        <field name="groups_id" eval="[(4, ref('benglish_academy.group_academic_manager'))]"/>
    </record>

</odoo>
