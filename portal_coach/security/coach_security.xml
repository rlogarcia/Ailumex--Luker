<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="0">
        <!-- Grupo de Coaches para acceso al portal -->
        <record id="group_benglish_coach" model="res.groups">
            <field name="name">Coach / Docente</field>
            <field name="category_id" ref="base.module_category_hidden"/>
            <field name="comment">Grupo para coaches que tienen acceso al portal de coaches</field>
        </record>
        
        <!-- Grupo alternativo (por compatibilidad) -->
        <record id="group_portal_coach" model="res.groups">
            <field name="name">Portal Coach</field>
            <field name="category_id" ref="base.module_category_hidden"/>
            <field name="comment">Grupo alternativo para coaches con acceso al portal</field>
        </record>
    </data>
</odoo>
